fix(kommo): fallback fetch direto se supabaseAdmin travar no webhook

- Promise.race com TIMEOUT_5S explícito para supabaseAdmin
- Se timeout disparar, tenta fetch direto à REST API do Supabase
- Logs distintos para cada cenário (query ok / timeout / fetch fallback)
- Resolve corte silencioso na busca da empresa MT Solar

Arquivos: api/index.ts
